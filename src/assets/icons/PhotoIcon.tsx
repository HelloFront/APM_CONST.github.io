import { Photo } from "tabler-icons-react";


const PhotoIcon = ({ size = 48, strokeWidth = 2, color = "black" }) => {
  return <Photo size={size} strokeWidth={strokeWidth} color={color} />;
};

export default PhotoIcon;
