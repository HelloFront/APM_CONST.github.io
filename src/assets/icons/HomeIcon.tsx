import { Home } from "tabler-icons-react";

const HomeIcon = ({ size = 48, strokeWidth = 2, color = "black" }) => {
  return <Home size={size} strokeWidth={strokeWidth} color={color} />;
};

export default HomeIcon;
