import { Article } from "tabler-icons-react";

const ArticleIcon = ({ size = 48, strokeWidth = 2, color = "black" }) => {
  return <Article size={size} strokeWidth={strokeWidth} color={color} />;
};

export default ArticleIcon;
