import { Bookmarks } from "tabler-icons-react";

const BookmarksIcon = ({ size = 48, strokeWidth = 2, color = "black" }) => {
  return <Bookmarks size={size} strokeWidth={strokeWidth} color={color} />;
};

export default BookmarksIcon;
